spring:
  security:
    user:
      name: user
      password: passwd
  datasource:
    url: jdbc:postgresql://localhost:5432/benew_test
    username: someUser
    password: somePass
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
springdoc:
  swagger-ui:
    enabled: true